/**
 * The shared presentational kit.
 *
 * One canonical home for the bricks every room renders with — extracted from the
 * 20 per-template `components/ui.tsx` copies, one pattern per brick-polish loop.
 * Import from `@/components/kit`, never re-implement.
 */
export { Avatar } from "./Avatar";
export { MoneyAmount } from "./MoneyAmount";
export { PersonChip } from "./PersonChip";
export { RecordRow } from "./RecordRow";
export { SplitView } from "./SplitView";
